# WS Server Demo Application (Apache CXF, generated interface)

## Running code generation from WSDL with Maven

    mvn clean generate-sources

## Build and run with Maven

    mvn clean package exec:java